/** Public entry point of @bantoozi/api: `buildServer()` for tests and `main.ts`. */
export const PACKAGE_NAME = '@bantoozi/api';

export { API_PREFIX, buildServer, type BuildServerOptions } from './server.js';
